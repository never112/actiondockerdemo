---
name: "pmdscan
author: "github-test-user"
description: "A GitHub Action pmd scan"
inputs:
  codePath:
    description: 'The code path.'
    default: ''
    required: false
runs:
  using: "docker"
  image: "Dockerfile"
  args:
    - ${{ inputs.codePath }}
branding:
  icon: "check-square"
  color: "white"
